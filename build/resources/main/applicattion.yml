# application.yml
server:
  port: 8084

spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/study?serverTimezone=UTC&characterEncoding=UTF-8
    username: ijeong0416
    password: 340834
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      properties:
        hibernate:
          default_batch_fetch_size: 100
          format_sql: true
    open-in-view: false
